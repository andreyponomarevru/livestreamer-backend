import { describe, it } from "@jest/globals";

describe("", () => {
  describe("destroyUser", () => {
    it.todo("destrpos the session");
    it.todo("clear the cookie");
    it.todo("closes the WebSocket connection");
  });
});
