import { describe, it } from "@jest/globals";

describe("parseBasicAuthorizationHeader", () => {
  describe("if the Authorization header is set", () => {
    it.todo("updates the Authorization header with the parsed one");
    it.todo("calls the next middleware");
  });

  describe("if the Authorization header is not set", () => {
    it.todo("calls the next middleware");
  });
});
