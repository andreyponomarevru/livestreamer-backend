import { describe, it } from "@jest/globals";

describe("scheduleService", () => {
  describe("scheduleBroadcast", () => {
    it.todo("saves and returns the scheduled broadcast");
  });

  describe("destroyScheduledBroadcast", () => {
    it.todo("deletes the scheduled broadcast");
  });

  describe("readAllScheduledBroadcasts", () => {
    it.todo("returns all scheduled broadcasts");
  });
});
