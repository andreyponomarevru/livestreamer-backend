import { describe, it } from "@jest/globals";

describe("mailService", () => {
  describe("sendEmail", () => {
    it.todo("sends the email");
    it.todo("returns the info about the email sent");
  });

  describe("createConfirmationEmail", () => {
    it.todo("returns the user sign up confirmation email");
  });

  describe("createWelcomeEmail", () => {
    it.todo("returns the welcome email");
  });

  describe("createPasswordResetEmail", () => {
    it.todo("returns the password-reset email");
  });
});
