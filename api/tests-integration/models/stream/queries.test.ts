import { describe, it } from "@jest/globals";

describe("streamRepo", () => {
  describe("create", () => {
    it.todo("creates a new broadcast stream");
  });

  describe("destroy", () => {
    it.todo("deletes the broadcast stream");
  });

  describe("read", () => {
    it.todo("returns the broadcast stream");
  });

  describe("readBroadcastId", () => {
    it.todo("returns the broadcast id");
  });

  describe("updateListenerPeakCount", () => {
    it.todo("updates the listener peak count");
  });

  describe("readListenerPeakCount", () => {
    it.todo("returns listener peak count");
  });

  describe("createLike", () => {
    it.todo("saves the like");
    it.todo("returns the saved like");
  });
});
