import { describe, it } from "@jest/globals";

describe("dbConnection", () => {
  describe("open", () => {
    it.todo("returns the connection");
  });

  describe("close", () => {
    it.todo("closes the connection");
  });
});
