import { describe, it } from "@jest/globals";

describe("redisConnection", () => {
  describe("open", () => {
    it.todo("opens the connections, returning the Redis client");
  });

  describe("end", () => {
    it.todo("forcibly close the connection");
  });

  describe("quit", () => {
    it.todo("closes the connection cleanly");
  });
});
