import WebSocket, { ServerOptions } from "ws";

import { describe, it } from "@jest/globals";

describe("ws service", () => {
  describe("onConnection event handler", () => {
    it("sends current broadcast state", () => {
      /*
        const server = new WebSocket("ws://localhost:5000");

      console.log(server);*/
    });
  });
});
